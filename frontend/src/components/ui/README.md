# UI Components

This folder is reserved for reusable and prebuilt UI components.

Current components:
- `HudPanel.tsx` for viewport overlays.
- `DisplayControls.tsx` for display and measure subtool controls.
